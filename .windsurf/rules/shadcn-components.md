---
trigger: always_on
---

Always remember to use shadcn's component, use the MCP server for shadcn in creating any UI. If a component already exists in shadcn's component, use that instead. And if the component does not exist in the codebase, just prompt so that I can install it manually.